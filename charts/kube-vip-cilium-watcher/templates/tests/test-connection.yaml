apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cilium-egress-observer.fullname" . }}-test-connection"
  labels:
    {{- include "cilium-egress-observer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cilium-egress-observer.fullname" . }}:8081/readyz']
  restartPolicy: Never
